{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Résultats de {{ user.firstname }} {{ user.lastname }}</h1>

    <table class="table">
        <tbody>
            <br>
            <tr>
                <th>Résultats</th>
                <td>{{ user.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <button type="button" class="btn btn-info">
    <a href="{{ path('user_index') }}">retour à la liste</a>
    </button>

{% endblock %}
