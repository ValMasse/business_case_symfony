{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ question.position }}</td>
            </tr>
            <tr>
                <th>Enonce</th>
                <td>{{ question.enonce }}</td>
            </tr>
            <tr>
                <th>EstEliminatoire</th>
                <td>{{ question.estEliminatoire ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>EstActif</th>
                <td>{{ question.estActif ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_index') }}">back to list</a>

    <a href="{{ path('question_edit', {'id': question.id}) }}">edit</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
