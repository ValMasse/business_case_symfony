<form method="post" action="{{ path('test_technique_delete', {'id': test_technique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_technique.id) }}">
    <button class="btn btn-danger float-right">Supprimer</button>
</form>
