<form method="post" action="{{ path('domaine_delete', {'id': domaine.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce domaine?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domaine.id) }}">
    <button class="btn btn-danger float-right">Supprimer</button>
</form>
