{% extends 'base.html.twig' %}

{% block title %}InfoCo index{% endblock %}

{% block body %}
    <h1>InfoCo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for info_co in info_cos %}
            <tr>
                <td>{{ info_co.id }}</td>
                <td>{{ info_co.date ? info_co.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('info_co_show', {'id': info_co.id}) }}">show</a>
                    <a href="{{ path('info_co_edit', {'id': info_co.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('info_co_new') }}">Create new</a>
{% endblock %}
